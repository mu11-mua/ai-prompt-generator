# 超详细部署指南 - 每一步点击什么

## 第一步：注册 GitHub 账号

1. 打开浏览器
2. 在地址栏输入：`github.com` 然后按回车
3. 看到 GitHub 首页后，点击右上角的 **"Sign up"** 按钮（绿色按钮）
4. 在 "Enter your email" 输入框中，输入你的邮箱
5. 点击 **"Continue"** 按钮
6. 在 "Create a password" 输入框中，设置密码（至少 8 位）
7. 点击 **"Continue"** 按钮
8. 在 "Enter a username" 输入框中，输入用户名（英文 + 数字）
9. 点击 **"Continue"** 按钮
10. 选择 "No" 或 "Yes"（是否接收邮件）
11. 点击 **"Continue"** 按钮
12. 完成拼图验证（拖动拼图块）
13. 去邮箱查收验证邮件
14. 点击邮件中的 **"Verify email address"** 按钮
15. 注册完成！

---

## 第二步：创建新仓库

1. 登录后，你会看到 GitHub 首页
2. 看页面**右上角**，找到 **"+"** 图标（在头像左边）
3. 点击 **"+"** 图标
4. 在弹出的菜单中，点击 **"New repository"**（第一个选项）
5. 进入创建页面后：
   - 找到 **"Repository name"** 输入框
   - 输入：`ai-prompt-generator`
   - 找到 **"Description"** 输入框（可选）
   - 输入：`AI 提示词生成器`
   - 找到 **"Public"** 圆圈，点击选中（必须选 Public）
   - **不要勾选** "Add a README file"（保持空白）
6. 看页面**最下方**，找到绿色按钮 **"Create repository"**
7. 点击 **"Create repository"** 按钮
8. 创建完成！

---

## 第三步：下载项目代码

1. 回到这个沙箱环境（Coze Coding）
2. 看页面**右上角**，找到 **"下载"** 或 **"Export"** 按钮
3. 点击 **"下载"** 按钮
4. 浏览器会下载一个 zip 文件
5. 找到下载的 zip 文件（通常在"下载"文件夹）
6. **右键点击** zip 文件
7. 选择 **"解压到当前文件夹"** 或 **"Extract Here"**
8. 解压后会得到一个文件夹

---

## 第四步：上传代码到 GitHub

1. 回到浏览器，打开刚才创建的 GitHub 仓库页面
2. 在页面中间，找到蓝色文字 **"uploading an existing file"**
3. 点击 **"uploading an existing file"** 链接
4. 进入上传页面后，你会看到一个大的拖拽区域
5. 打开刚才解压的文件夹
6. **全选所有文件**：
   - Windows: 按 `Ctrl + A`
   - Mac: 按 `Cmd + A`
7. **拖拽到上传区域**：
   - 用鼠标选中所有文件
   - 按住鼠标左键
   - 拖到浏览器的上传区域
   - 松开鼠标
8. 等待上传进度条完成
9. 看页面**最下方**：
   - 找到 **"Commit changes"** 输入框
   - 输入：`Initial commit`
   - 找到绿色按钮 **"Commit changes"**
10. 点击 **"Commit changes"** 按钮
11. 上传完成！

---

## 第五步：注册 Vercel 账号

1. 打开浏览器新标签页
2. 在地址栏输入：`vercel.com` 然后按回车
3. 看到 Vercel 首页后，点击右上角的 **"Sign Up"** 按钮
4. 在登录方式中，找到 **"Continue with GitHub"** 按钮
5. 点击 **"Continue with GitHub"** 按钮
6. 会跳转到 GitHub 授权页面
7. 找到绿色的 **"Authorize Vercel"** 按钮
8. 点击 **"Authorize Vercel"** 按钮
9. 注册完成！

---

## 第六步：部署项目

1. 登录后，你会看到 Vercel 首页
2. 看页面**右上角**，找到 **"Add New..."** 按钮
3. 点击 **"Add New..."** 按钮
4. 在弹出的菜单中，点击 **"Project"**（第一个选项）
5. 进入 "Import Git Repository" 页面
6. 在仓库列表中，找到 **"ai-prompt-generator"**
7. 点击右边的 **"Import"** 按钮
8. 进入配置页面后：
   - 找到 **"Framework Preset"** 下拉框
   - 点击下拉框，选择 **"Next.js"**
   - 找到 **"Build Command"** 输入框
   - 输入：`pnpm build`
   - 找到 **"Output Directory"** 输入框
   - 输入：`.next`
   - 找到 **"Install Command"** 输入框
   - 输入：`pnpm install`
9. 看页面**最下方**，找到蓝色按钮 **"Deploy"**
10. 点击 **"Deploy"** 按钮
11. 等待 2-3 分钟（页面会显示部署进度）
12. 看到 **"Congratulations!"** 字样，部署完成！

---

## 第七步：获取永久链接

1. 部署完成后，页面会显示一个预览图
2. 看预览图**上方**，找到链接（格式类似）：
   ```
   https://ai-prompt-generator-xxx.vercel.app
   ```
3. 链接右边有一个 **"复制"** 图标
4. 点击复制图标，复制链接
5. 或者直接用鼠标选中链接，按 `Ctrl+C`（Windows）或 `Cmd+C`（Mac）
6. 链接已复制！

---

## 第八步：在微信中测试

1. 打开微信电脑版或手机版
2. 找到 **"文件传输助手"**（或任意聊天窗口）
3. 在输入框中，按 `Ctrl+V`（Windows）或 `Cmd+V`（Mac）粘贴链接
4. 点击 **"发送"** 按钮
5. 点击发送的链接
6. 浏览器会打开你的应用
7. 测试功能是否正常

---

## 完成！🎉

现在你可以：
- 把链接发给任何人
- 在微信中随时打开
- 链接永久有效
- 完全免费

---

## 如果遇到问题

### 问题 1：找不到按钮
**解决：** 截图给我看，我告诉你点哪里

### 问题 2：部署失败
**解决：** 
1. 在 Vercel 页面，点击 **"Deployments"** 标签
2. 找到失败的部署，点击 **"..."** 按钮
3. 选择 **"View Build Logs"**
4. 截图错误信息给我

### 问题 3：链接打不开
**解决：**
1. 等待 5 分钟（可能需要时间生效）
2. 刷新页面（按 F5）
3. 检查仓库是否是 Public（公开）

---

## 需要帮助？

任何步骤卡住了：
1. 截图给我看当前页面
2. 告诉我你在第几步
3. 我告诉你下一步点哪里
